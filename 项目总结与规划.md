# EduSpark 项目开发记录

> 基于大模型的个性化教育多智能体系统
> 最后更新: 2026-06-08

---

## 一、项目定位

一句话：**一个能通过 AI 对话了解学生学习特点，然后自动生成定制化学习内容和路径的教育平台。**

目标课程：机器学习（面向大三计算机系学生）

---

## 二、当前完成情况

### ✅ 后端基础设施

| 项目 | 状态 | 说明 |
|------|------|------|
| FastAPI 框架 | 完成 | 分层架构：api → agents → services → models |
| MySQL 数据库 | 完成 | 6 张表（users, student_profiles, resources, learning_paths, learning_records, chat_histories） |
| DeepSeek 集成 | 完成 | 使用 deepseek-v4-flash，OpenAI 兼容格式 |
| 用户认证 | 完成 | 注册/登录/JWT token |
| Conda 环境 | 完成 | Python 3.11 + 所有依赖 |

### ✅ 核心 Agent

| Agent | 状态 | 说明 |
|-------|------|------|
| **Orchestrator（编排器）** | ✅ 完成 | 用 LLM 判断用户意图，分流到对应 Agent |
| **ProfileAgent（画像分析）** | ✅ 已完成 | 6 维度画像提取（知识基础、认知风格、学习能力、易错点、目标、偏好），每轮对话增量更新。已完善分阶段追问策略、字段映射对齐、智能增量合并 |
| DocumentAgent（文档生成） | ⚠️ 骨架 | 提示词定义好，未完整测试 |
| QuizAgent（题目生成） | ⚠️ 骨架 | 同上 |
| MindMapAgent（思维导图） | ⚠️ 骨架 | 同上 |
| CodeAgent（代码案例） | ⚠️ 骨架 | 同上 |
| VideoScriptAgent（视频脚本） | ⚠️ 骨架 | 同上 |
| PathAgent（路径规划） | ⚠️ 骨架 | 同上 |

### ✅ 前端

| 页面 | 状态 | 说明 |
|------|------|------|
| 登录/注册 | 完成 | 基础功能可用，UI 待重做 |
| 智能对话 | 完成 | SSE 流式对话 + Agent 状态显示 + 历史记录侧栏 |
| 学习画像 | 完成 | 雷达图 + 详情描述 |
| 学习资源 | 完成 | 卡片列表 + 分类筛选 |
| 学习路径 | 完成 | 时间线展示 |

---

## 三、技术栈

### 后端
- Python 3.11 + FastAPI
- SQLAlchemy + PyMySQL（MySQL）
- DeepSeek V4 Flash（大模型）
- ChromaDB（向量存储，未启用）
- SSE（服务端推送）

### 前端
- Vue 3 + Vite
- Pinia（状态管理）
- Element Plus（UI 组件库）
- ECharts（雷达图）
- Markdown-it + highlight.js（Markdown 渲染）

---

## 四、架构总览

```
用户 → Web 前端 (Vue 3)
         ↓ API (HTTP/SSE)
       FastAPI 后端
         ↓
   Orchestrator（编排器）
         ↓ 意图分流
   ┌──────┼──────┐
   │      │      │
画像Agent 资源Agent群 路径Agent
   │      │      │
   └──────┼──────┘
         ↓
     DeepSeek V4 Flash
         ↓
      MySQL 数据库
```

### 完整用户流程

```
用户注册/登录
    ↓
在对话页面发消息 → SSE 流式连接
    ↓
Orchestrator 判断意图
    ↓
├─ 自我介绍、普通聊天 → ProfileAgent（画像分析）
│    ├ 提取 6 维度画像 → 存 student_profiles 表
│    └ SSE 推送：chunk（聊天内容）+ profile_updated + done
│
├─ 生成资源 → ResourceAgents（5 个并发生成）
│    ├ 文档/题目/思维导图/代码/视频
│    ├ 存 resources 表
│    └ SSE 推送：agent_status（实时进度）+ resource_generated + done
│
├─ 规划路线 → PathAgent
│    ├ 生成带步骤的学习路径
│    ├ 存 learning_paths 表
│    └ SSE 推送：path_generated + done
│
└─ 知识提问 → ProfileAgent（只回答，不追问画像）
```

---

## 五、还缺什么（待办）

### 🔴 高优先级 — 核心功能补全

| 任务 | 说明 |
|------|------|
| **Tutor Agent（智能辅导）** | 目前完全没有。学生问知识点时，应该能基于历史 + 画像做针对性辅导 |
| **Evaluation Agent（效果评估）** | 目前完全没有。评估学生掌握程度，调整学习策略 |
| **资源生成 Agent 调优 & 测试** | 5 个 Agent 的骨架已写，需要连线完整测试一轮 |
| **路径规划 Agent 调优 & 测试** | 同上 |
| **意图分类优化** | 目前用 LLM 分类，但还有些边界情况不准。已部分修复 |

### 🟡 中优先级 — 增强功能

| 任务 | 说明 |
|------|------|
| **RAG 知识库（ChromaDB）** | 上传教材 → 切块 → 转向量 → 存 ChromaDB。用户提问时检索相关段落喂给 Agent |
| **文件上传接口** | 支持上传 PDF/PPT/Word |
| **多轮画像迭代优化** | 对话越多，画像越准。目前简单的增量更新 |

### 🟢 低优先级 — 打磨

| 任务 | 说明 |
|------|------|
| **前端 UI 重构** | 见 UI_重构需求.md。整体翻新，科技感设计 + 交互动效 |
| **MySQL → 生产配置优化** | 连接池、索引、慢查询优化 |
| **部署文档** | 一键部署脚本 |
| **演示视频** | 项目展示用 |

---

## 六、最终做成什么样

一个学生打开网站，注册登录后看到一个**有科技感但不花哨**的对话界面。

1. **第一次使用**：他会收到 AI 助手的主动问好，AI 通过自然聊天了解他的专业背景、学习目标、偏好
2. **画像素描**：AI 在对话中逐步构建他的 6 维学习画像（知识基础、认知风格、学习能力、易错点、目标、偏好），画像会在学习画像页面实时可视化
3. **资源生成**：当他说"帮我生成决策树的讲解文档"，AI 并行生成 5 种资源（文档、题目、思维导图、代码、视频脚本），整个过程通过 SSE 实时展示进度
4. **路径规划**：当他说"帮我规划学习路线"，AI 根据画像生成个性化的学习路径，按知识依赖排序
5. **持续学习**：他边学边问，AI 辅导他不懂的知识点。随着对话增多，画像越来越准确，推荐越来越个性化
6. **效果评估**：系统定期评估他的掌握程度，动态调整学习策略

最终交付物：一个**可演示、可运行**的全栈 Web 应用，后端 7 个 Agent 协作，前端可视化展示画像、资源、路径，支持流式对话。
